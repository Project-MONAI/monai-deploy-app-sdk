[project]
name = "cchmc-nnunet-fast"
version = "0.0.1"
description = "Fast-track nnU-Net segmentation MAP for CCHMC models"
requires-python = ">=3.10,<3.13"
dependencies = [
    "monai-deploy-app-sdk",
]

# Placeholder — to be populated in Task 0.2 with full dependency list
# and build-system configuration.

[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.backends._legacy:_Backend"
